// TODO(Zemcov): import 'package:json_annotation/json_annotation.dart';

class NoteData {
  final String title;
  final DateTime dateTime;

  NoteData({
    required this.title,
    required this.dateTime,
  });

// TODO(Zemcov): fromJson, toJson.
}
